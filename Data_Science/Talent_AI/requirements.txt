# CPU-only PyTorch. sentence-transformers pulls in torch as a dependency, and
# without this, pip resolves the default CUDA-enabled build -- several GB of
# GPU libraries never used here (same fix as the Dockerfile; confirmed by live
# testing to cut the Docker image from ~10GB to ~3.4GB). Needed for a viable
# Streamlit Community Cloud deploy too, not just Docker.
--extra-index-url https://download.pytorch.org/whl/cpu
torch

# Phase 1: parsing -> extraction -> embeddings -> ranking
pymupdf
pytesseract
pdf2image
spacy
# en_core_web_sm as a direct wheel URL, not a separate `python -m spacy download`
# step -- Streamlit Community Cloud only ever runs `pip install -r requirements.txt`,
# it has no hook for a post-install command, so the model has to be installable
# as a normal pip package. Version pinned to match the spacy 3.8.x resolved above
# (spaCy models are versioned by compatible spaCy release, not exact-pinned).
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.8.0/en_core_web_sm-3.8.0-py3-none-any.whl
sentence-transformers
faiss-cpu
scikit-learn
pandas
pyarrow
pydantic
python-dotenv
kagglehub
pytest
jupyter

# Phase 2: AI candidate insights (summaries, missing skills, interview questions)
openai

# Phase 3: recruiter dashboard
streamlit

# Phase 4: automation (folder watcher, scheduled re-ranking). Docker/SMTP need no
# extra Python packages (Docker isn't a pip dep; email uses the smtplib stdlib).
watchdog
